<h1><%= t('event.index.title') %></h1>

<div class="my-3">
  <%= link_to t('event.index.btn_create_event'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row mb-3">
  <%# render array of events, each in the 'events/event'  %>
  <%= render partial: 'events/event', collection: @events %>
</div>
