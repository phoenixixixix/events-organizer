<h1><%= t('.title') %></h1>

<div class="my-3">
  <%= link_to t('.btn_create_event'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row mb-3">
  <%# render array of events, each in the 'events/event'  %>
  <%= render partial: 'events/event', collection: @events %>
</div>
